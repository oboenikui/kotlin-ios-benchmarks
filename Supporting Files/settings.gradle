pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url="https://dl.bintray.com/kotlin/kotlin-eap" }
    }
}

include(":KotlinNativeBenchmark")
project(":KotlinNativeBenchmark").projectDir = file("../KotlinNativeBenchmark")
rootProject.name = file("..").name
